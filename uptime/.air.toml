root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./cmd/uptime"
bin = "./tmp/main"
include_ext = ["go", "toml", "yaml", "yml"]
exclude_dir = ["tmp"]
# fsnotify не работает через volume-mount под Docker Desktop на Windows — опрос файлов
poll = true
poll_interval = 500
